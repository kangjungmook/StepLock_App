# 스텝락 디자인 기준

이 문서는 **코드에 이미 있는 값**을 정리한 것이 우선이고, 비어 있던 부분만 새로 제안합니다.
두 가지를 섞지 않기 위해 모든 항목에 출처를 붙였습니다.

| 표시 | 뜻 |
| --- | --- |
| **[코드]** | 구현 파일에서 값을 직접 확인했습니다. 바꾸면 화면이 바뀝니다 |
| **[화면]** | 렌더한 화면 이미지에서 눈으로 확인했습니다 |
| **[제안 · 시작값]** | 정해진 값이 없어 이 문서에서 처음 정합니다. **확정이 아니라 출발점**이고, 실제 기기에서 보고 조정해야 합니다 |

> **화면 확인의 한계**
> `docs/screens/`의 이미지는 실제 기기 캡처가 아니라, 같은 토큰·간격으로 **HTML로 다시 렌더한 것**입니다.
> 도형과 색·간격은 코드와 1:1로 맞췄지만, 폰트 렌더링·리플·애니메이션은 기기에서만 확인됩니다.
> 이 문서에서 **[화면]**은 "레이아웃과 색 조합을 눈으로 봤다"는 뜻이고, 기기 검증이 아닙니다.

## 플랫폼 범위

**Android만 구현되어 있습니다.** Kotlin + Jetpack Compose, 단일 모듈(`app/`).
**iOS 코드는 이 저장소에 없습니다.** 이 앱의 핵심 기능(전경 앱 감지 + 다른 앱 위에 덮기)은
iOS에서 허용되지 않아 같은 구조로 옮길 수 없습니다. 그래서 iOS 항목은 전부
**[제안 · 시작값]**이며, 나중에 Screen Time(`FamilyControls`) 기반의 **별도 앱**을 만들 때
쓸 수 있는 환산값입니다.

**단위**

- Android: 글자 `sp`, 그 외 `dp`
- iOS: 전부 `pt`
- 이 앱은 1dp ≈ 1pt로 환산합니다. 값이 그대로 옮겨진 항목은 따로 적지 않았습니다.

**파일 위치**

| 무엇 | 어디 |
| --- | --- |
| 색 | `app/src/main/java/com/steplock/app/ui/theme/Color.kt` |
| 글꼴·글자 크기 | `.../ui/theme/Type.kt` |
| 간격·크기·모서리 | `.../ui/theme/Dimens.kt` |
| 공통 컴포넌트 | `.../ui/components/` |
| 화면 | `.../ui/screens/` |

---

# 1. 컬러

## 1-1. 설계 방식 **[코드]**

색은 **OKLCH로 설계하고 sRGB로 변환**해 씁니다. 원본 OKLCH 값은 `Color.kt`에 주석으로
남아 있어 시안과 대조할 수 있습니다.

```kotlin
val Background = Color(0xFFF5F2E9) // oklch(96% 0.012 90)
```

두 가지 원칙이 이미 지켜지고 있습니다.

- **순수 흰색·검정을 쓰지 않습니다.** 배경은 브랜드 색이 은은하게 섞인 **틴티드 뉴트럴**입니다.
  라이트 배경은 `#F5F2E9`(따뜻한 크림), 다크 배경은 `#0F141D`(파랑이 섞인 짙은 회색)입니다.
- **다크 팔레트는 잠금 화면 전용**이고 시스템 다크모드를 따라가지 않습니다.

## 1-2. 라이트 (기본) **[코드]**

| 역할 | 토큰 | OKLCH | HEX |
| --- | --- | --- | --- |
| 화면 배경 | `SlColor.Background` | `oklch(96% 0.012 90)` | `#F5F2E9` |
| 카드·패널 면 | `SlColor.Surface` | `oklch(99% 0.006 90)` | `#FDFCF7` |
| 보조 면 (계정 줄 등) | `SlColor.SurfaceAlt` | `oklch(93% 0.02 90)` | `#EDE8D9` |
| 테두리 | `SlColor.Border` | `oklch(88% 0.015 90)` | `#DBD7CD` |
| 진한 테두리 (목표 점선) | `SlColor.BorderStrong` | `oklch(80% 0.015 90)` | `#C1BDB3` |
| 스위치 꺼짐 트랙 | `SlColor.TrackOff` | `oklch(85% 0.015 90)` | `#D1CDC3` |
| 본문 | `SlColor.TextPrimary` | `oklch(23% 0.02 260)` | `#171D26` |
| 보조 글 | `SlColor.TextSecondary` | `oklch(50% 0.02 260)` | `#5D646F` |
| 흐린 글·셰브론 | `SlColor.TextTertiary` | `oklch(58% 0.02 260)` | `#737B87` |

**대표색 (브랜드 그린)**

| 역할 | 토큰 | HEX | 쓰는 곳 |
| --- | --- | --- | --- |
| 대표색 | `Brand` | `#009267` | 주 버튼, 진행 링, 캐릭터 |
| 진한 톤 | `BrandInk` | `#006944` | 링크 글자, 캐릭터 그림자 면 |
| 가장 진한 톤 | `BrandDeep` | `#005A38` | 작은 링 안 숫자, 아이콘 |
| 대표색 위 글자 | `OnBrand` | `#F6FEFA` | 주 버튼 글자 |
| 연한 배경 | `BrandTint` | `#C7F0DC` | 홈 상태 카드(열림) |
| 연한 배경 변형 | `BrandTintAlt` | `#CDF2E0` | 칩 배경 |

## 1-3. 완료 · 주의 · 오류 **[코드]**

역할이 셋으로 나뉘어 있고 색도 따로입니다. **억지로 하나로 묶지 않았습니다.**

| 상태 | 뜻 | 토큰 | HEX |
| --- | --- | --- | --- |
| **완료** | 조건을 채웠다 | `Brand` / `BrandTintAlt`(배경) | `#009267` / `#CDF2E0` |
| **주의** | 아직 잠겨 있다 (나쁜 일이 아님) | `AmberSurface`(면) · `AmberText`(글) · `AmberSubText`(보조) | `#FFE7C3` / `#492B0F` / `#6A4F38` |
| **오류** | 실패했다, 고쳐야 한다 | `Error` / `OnError`(그 위 글자) | `#C53637` / `#FFF9F8` |

- **주의(앰버)와 오류(빨강)를 구분합니다.** "아직 잠겨 있어요"는 정상 상태이므로 빨강을 쓰지
  않고 따뜻한 앰버를 씁니다. 빨강은 **권한이 꺼져 기능이 멈춤**, **계정 삭제**, **입력 오류**에만 씁니다.
- `Error` 위의 흰 글자 대비는 **5.2:1**로 측정해 두었습니다 (`Color.kt` 주석).

**식별 목적으로 브랜드 색을 유지하는 예외** **[코드]**
앱 뱃지(쇼츠 `#DB4241`, 릴스 `#BF4392`, 틱톡 `#313B4A`, 엑스 `#2A75BA`)와
소셜 로그인 아이콘(구글 4색, 카카오 `#F6D653`)만 각 서비스 색을 씁니다.
**팔레트 규칙의 예외이고, 새로 늘리지 않습니다.**

## 1-4. 다크 (잠금 화면 전용) **[코드]**

| 역할 | 토큰 | HEX |
| --- | --- | --- |
| 배경 | `Dark.Background` | `#0F141D` |
| 면 | `Dark.Surface` | `#1C222B` |
| 보조 면 (버튼·링 트랙) | `Dark.SurfaceAlt` | `#212730` |
| 테두리 | `Dark.Border` | `#2F3640` |
| 본문 | `Dark.TextPrimary` | `#F4F2EA` |
| 보조 글 | `Dark.TextMuted` | `#9DA5B1` |
| 링크 | `Dark.TextLink` | `#9199A5` |
| 진행 링 (앰버) | `Dark.AmberRing` | `#E59656` |
| 강조 글 (앰버) | `Dark.AmberText` | `#FBA962` |
| 캐릭터 본체 | `Dark.GreenIcon` | `#3CC998` |
| 캐릭터 그림자 면 | `Dark.GreenDeep` | `#0D8963` |

**다크에서 지키는 것** **[코드]**

- 순수 검정(`#000000`) 금지. 파랑이 섞인 짙은 회색을 씁니다.
- 라이트 값을 기계적으로 반전하지 않았습니다. 다크 전용 값을 따로 뽑았습니다.
- 가장 넓은 면적(진행 링)은 채도를 낮췄습니다 — 라이트 앰버 `0.15` → 다크 링 `0.125`.
  `Color.kt` 주석에 이유가 적혀 있습니다.

## 1-5. 눌림 색 **[제안 · 시작값]**

지금 눌림 상태에 **전용 색이 없습니다.** `Button`은 Material3 기본 리플,
`Modifier.clickable`을 쓰는 줄도 기본 리플을 씁니다 **[코드]**.
기기에서 보고 어색하면 아래를 시작값으로 쓰세요.

| 역할 | 시작값 | 근거 |
| --- | --- | --- |
| 대표색 버튼 눌림 | `oklch(52% 0.13 165)` = `#007F56` | `Brand`에서 L만 −6% |
| 면 눌림 (목록 줄) | `oklch(95% 0.012 90)` = 배경과 면 사이 | 리플 대신 면 색 변화 |
| 다크 면 눌림 | `oklch(32% 0.02 260)` = `#2D333D` | `Dark.SurfaceAlt`에서 L +5% |

**먼저 기본 리플로 기기에서 확인하고, 문제가 없으면 이 토큰을 추가하지 마세요.** 쓰지 않는
토큰은 혼란만 늘립니다.

---

# 2. 타이포그래피

## 2-1. 글꼴 **[코드]**

| | 값 |
| --- | --- |
| 기본 글꼴 | **Noto Sans KR** (산세리프) |
| 굵기 | 400 Normal · 500 Medium · 700 Bold · 900 Black |
| 받는 방식 | Google Play 서비스 다운로더블 폰트 (`com.google.android.gms.fonts`) |
| 인증서 | `res/values/font_certs.xml` (AOSP 샘플 파일) |
| 대체 글꼴 | Play 서비스가 없으면 **시스템 기본**으로 떨어집니다. 한국어 기기에서는 대개 Noto Sans CJK KR이라 형태는 비슷하지만 **Black(900)이 없어 Bold로 합성**됩니다 |

세리프는 쓰지 않습니다. 숫자가 많은 앱이라 한 글꼴의 굵기 차이로 위계를 만듭니다.

**ttf를 번들하려면** `res/font`에 넣고 `Type.kt`의 `NotoSansKr` 정의만 교체하면 됩니다 —
호출부는 전부 `SlText.*`를 쓰므로 바뀌지 않습니다.

## 2-2. 공통 규칙 **[코드]**

```kotlin
platformStyle = PlatformTextStyle(includeFontPadding = false)
lineHeightStyle = LineHeightStyle(alignment = Center, trim = None)
```

- `includeFontPadding = false` — 한글 글꼴의 위아래 여백을 없애 시안 행간과 맞춥니다.
- 행간 기본값은 **글자 크기 × 1.35**이고, 제목은 더 좁게 직접 지정합니다.
- 큰 제목은 **자간을 음수로** 좁힙니다 (`−0.4` ~ `−1`). 섹션 라벨만 `+0.6`으로 넓힙니다.

## 2-3. 크기 표 **[코드]**

Android는 `sp`, iOS는 `pt`입니다. 아래 값은 전부 코드에서 확인한 실제 값입니다.

### 제목 (Black 900)

| 토큰 | 크기/행간 | 자간 | 쓰는 곳 |
| --- | --- | --- | --- |
| `RingValue` | 42 / 42 | −1 | 잠금 화면 퍼센트 |
| `AppTitle` | 34 / 39 | −0.6 | 온보딩 "스텝락" |
| `Wordmark` | 32 / 36 | −1 | 로그인 워드마크 |
| `LockTitle` | 25 / 32 | −0.4 | 잠금 화면 제목 |
| `Greeting` | 24 / 31 | −0.4 | 홈 인사말, 통계 제목, 연속 일수 |
| `LoginHeading` | 20 / 26 | 0 | 로그인 "다시 오셨네요" |
| `DialogTitle` | 19 / 26 | 0 | 확인 대화상자 제목 |
| `StatusTitle` | 18 / 25 | 0 | 홈 상태 카드 제목, 연속 일수 |

### 강조 (Bold 700)

| 토큰 | 크기/행간 | 쓰는 곳 |
| --- | --- | --- |
| `ScreenTitle` | 18 / 24 | 화면 상단 제목 (설정) |
| `StepperValue` | 17 / 22 | 목표값 숫자 |
| `Cta` | 16 / 22 | 버튼 글자 |
| `RowTitle` | 15 / 21 | 목록 줄 제목 |
| `Remaining` | 14 / 20 | "2,760보만 더 걸으면 열려요" |
| `Chip` | 12 / 16 | 칩, 연속 배지 |
| `SectionLabel` | 12 / 16 (자간 +0.6) | 섹션 라벨 |
| `RingPercent` | 11 / 14 | 작은 링 안 퍼센트 |

### 본문 (Normal 400)

| 토큰 | 크기/행간 | 쓰는 곳 |
| --- | --- | --- |
| `Tagline` | 15 / 24 | 온보딩 설명 |
| `Body` | 14 / 22 | 본문, 대화상자 설명 |
| `Input` | 14 / 20 | 입력 칸 |
| `BodySm` | 13 / 20 | 상태 카드 설명 |
| `RowValue` | 13 / 18 | 목록 줄 값 |
| `Caption` | 12 / 19 | 캡션 |

### 보조 (Medium 500)

| 토큰 | 크기/행간 | 쓰는 곳 |
| --- | --- | --- |
| `ListItem` | 15 / 20 | 앱 목록 이름 |
| `StepperLabel` | 14 / 20 | 스테퍼 라벨 |
| `Link` / `LinkSm` | 13.5 / 13 | 텍스트 링크 |
| `LabelSm` | 12 / 16 | 작은 라벨 |
| `NavLabel` | 11 / 14 | 하단 탭 라벨 |

## 2-4. iOS 환산 **[제안 · 시작값]**

| 역할 | Android | iOS 시작값 |
| --- | --- | --- |
| 화면 제목 | `Greeting` 24sp Black | 24pt, `.heavy` |
| 카드 제목 | `StatusTitle` 18sp Black | 18pt, `.heavy` |
| 줄 제목 | `RowTitle` 15sp Bold | 15pt, `.semibold` |
| 본문 | `Body` 14sp Normal | 14pt, `.regular` |
| 보조 | `RowValue` 13sp Normal | 13pt, `.regular` |
| 라벨 | `LabelSm` 12sp Medium | 12pt, `.medium` |

- iOS는 **Dynamic Type**을 고려해야 하므로 고정 pt보다 `UIFontMetrics`로 스케일하는 것을
  권합니다. 위 값은 기본 크기(Large) 기준입니다.
- iOS에서도 Noto Sans KR을 번들해 같은 글꼴을 쓰고, 없으면 시스템(SF Pro + Apple SD Gothic Neo)으로 떨어뜨립니다.

---

# 3. 레이아웃

## 3-1. 간격 격자 **[코드]**

**4dp 격자만 사용합니다.** 실제로 쓰이는 값: `4 · 6 · 8 · 10 · 12 · 14 · 16 · 20 · 24 · 28 · 32`

> `6`, `10`, `14`는 4의 배수가 아니지만 **2dp 단위의 미세 조정**으로 이미 쓰이고 있습니다
> (글자 사이 6dp, 칩 안쪽 10dp, 목록 줄 14dp). 새로 값을 정할 때는 4의 배수를 먼저 쓰세요. **[코드]**

## 3-2. 화면 좌우 여백 **[코드 + 이번에 정리]**

조사해 보니 **20 · 24 · 28dp 세 값**이 쓰이고 있었습니다. 역할이 나뉘는 두 값은 이름을 붙여
남기고, 하나뿐이던 28dp는 같은 역할이라 통일했습니다.

| 토큰 | 값 | 쓰는 화면 | 근거 |
| --- | --- | --- | --- |
| `ScreenPadding` | **20dp** | 홈 · 설정 · 통계 · 집중 타이머 | 하단 탭이나 목록이 있어 내용이 빽빽한 화면 |
| `ScreenPaddingWide` | **24dp** | 로그인 · 온보딩 · 잠금 | 하단 탭 없이 가운데 정렬된 내용 — 여백을 더 줍니다 |

**바꾼 것**: 로그인만 쓰던 28dp를 24dp로, 온보딩·잠금의 하드코딩 24dp를 토큰으로.

## 3-3. 요소 사이 간격 **[코드]**

| 무엇 | 값 |
| --- | --- |
| 섹션 사이 (섹션 라벨 위) | **28dp** |
| 섹션 라벨 → 패널 | **12dp** |
| 카드 사이 | **16dp** |
| 제목 → 설명 | **6~8dp** |
| 설명 → 다음 블록 | **24~32dp** |
| 목록 줄 위아래 | **12dp**(부제 없음) / **14dp**(부제 있음) |
| 아이콘 → 글자 | **12~14dp** |

## 3-4. 안쪽 여백 · 모서리 **[코드]**

| 토큰 | 값 | 쓰는 곳 |
| --- | --- | --- |
| `PanelPadding` | 16dp | 패널·카드 안쪽 |
| `RadiusPanel` | **20dp** | 큰 면 — 홈 상태 카드, 온보딩 규칙 패널, 대화상자 |
| `RadiusCard` | **18dp** | 카드·목록 패널 — `SlPanel` 기본, 조건 설정 카드, 경고 줄 |
| `RadiusCta` | **16dp** | 버튼 |
| `RadiusField` | **14dp** | 입력 칸, 작은 아이콘 타일 |
| `RadiusBadge` / `RadiusBadgeSmall` | 13dp / 12dp | 앱 뱃지 (40dp / 36dp) |
| `RadiusSmall` | 8dp | 체크박스, 텍스트 링크의 리플 영역 |
| 원형 | `CircleShape` | 칩, 소셜 버튼, 점, 스위치 |

**모서리는 요소 크기에 따라 커집니다** (8 → 12 → 14 → 16 → 18 → 20). 같은 크기의 요소가
다른 모서리를 쓰지 않도록 위 표에서 고르세요. **[코드]**

## 3-5. 버튼·터치 크기 **[코드]**

| 토큰 | 값 | 뜻 |
| --- | --- | --- |
| `TouchTarget` | **44dp** | **모든 누를 수 있는 요소의 최소 크기** |
| `CtaHeight` | **56dp** | 주 버튼 높이 (전 화면 공통) |
| `SwitchWidth` × `SwitchHeight` | 52 × 32dp | 스위치 트랙 |
| `SwitchKnob` | 26dp | 스위치 손잡이 |

**바꾼 것**: 로그인 버튼만 52dp였습니다. 같은 역할이므로 56dp로 통일하고
쓰이지 않게 된 `ButtonHeight` 토큰을 지웠습니다.

**44dp는 시각 크기와 별개로 확보합니다** — 텍스트 링크·체크박스·스테퍼 버튼은
보이는 크기가 작아도 `defaultMinSize(minHeight = TouchTarget)`로 터치 영역을 넓혔습니다. **[코드]**

## 3-6. 정렬 **[코드 + 화면]**

- 기본은 **왼쪽 정렬**입니다. 제목·목록·설정 모두 왼쪽에 붙습니다.
- **가운데 정렬은 세 곳만**: 잠금 화면 전체, 집중 타이머, 온보딩 캡션.
  "지금 이것만 보라"는 화면에서만 씁니다.
- 목록 줄은 `[앞쪽 시각 요소] [제목/값 — 남는 공간] [뒤쪽 상태]` 3칸 구조입니다.
- 숫자는 `font-variant-numeric: tabular-nums`에 해당하는 처리를 하지 않았습니다.
  타이머처럼 매초 바뀌는 숫자가 흔들릴 수 있습니다 **[제안 · 시작값]**:
  `PomodoroScreen`의 남은 시간에 고정폭 숫자를 적용하세요.

## 3-7. iOS 환산 **[제안 · 시작값]**

| 역할 | Android | iOS 시작값 |
| --- | --- | --- |
| 화면 좌우 여백 | 20dp / 24dp | 16pt / 20pt — iOS는 관례상 16pt가 표준이라 한 단계 좁힙니다 |
| 최소 터치 | 44dp | **44pt** (애플 HIG와 같은 값) |
| 주 버튼 높이 | 56dp | 50pt |
| 카드 모서리 | 18dp | 12pt — iOS는 더 작은 모서리가 자연스럽습니다 |
| 큰 면 모서리 | 20dp | 16pt |
| 섹션 사이 | 28dp | 24pt |

---

# 4. 공통 컴포넌트

전부 `ui/components/`에 있고 **화면에서 직접 그리지 않습니다.** 같은 역할이면 이 목록에서
고르고, 없으면 추가한 뒤 이 문서에 적으세요.

## 4-1. 버튼 · 링크 **[코드]**

| 컴포넌트 | 모양 | 크기 | 쓰는 기준 |
| --- | --- | --- | --- |
| `PrimaryButton` | 꽉 찬 면, 모서리 16dp | 높이 **56dp**, 가로 꽉 채움 | **화면의 주 동작 하나.** 색을 바꿔 파괴적 동작(삭제=`Error`)이나 다크 화면(`Dark.SurfaceAlt`)에도 씁니다 |
| `TextLink` | 글자만, 밑줄 선택, 리플 8dp 모서리 | 터치 영역 **44dp** | 부차적 동작. 밑줄 켜면 문서 링크 느낌, 끄면 줄 안의 동작(로그아웃 등). **줄 안에 인라인으로도 씁니다** — 로그인의 "회원가입", "비밀번호를 잊으셨나요?" |
| `IconTapTarget` | 원형 면 + 아이콘 | **44dp** | 뒤로 가기 같은 아이콘 단독 버튼 |
| `SocialLoginButton` | 원형 | **44dp** | 구글·카카오·애플. 각 브랜드 색 유지 |
| `GoalStepper` | −/+ 버튼 + 숫자 | 버튼 **44dp** | 목표값 조절. 걸음 500보 · 수면 30분 · 세션 1회 단위 |

**주 버튼은 한 화면에 하나입니다.** 잠금 화면처럼 두 동작이 있으면
주 동작만 `PrimaryButton`, 나머지는 `TextLink`로 둡니다 **[화면]**.

## 4-2. 면 · 카드 **[코드]**

| 컴포넌트 | 모양 | 쓰는 기준 |
| --- | --- | --- |
| `SlPanel` | `Surface` 면 + 1dp `Border` + 모서리 18dp | **카드를 겹치지 않고** 하나의 면 안에서 줄을 나눕니다. `containerColor` · `borderColor` 로 변형합니다 — 보조 면은 `SurfaceAlt` + `Transparent`, 경고는 `Surface` + `Error` |
| `SlDivider` | 1dp `Border` 선 | 패널 안 줄 구분. **패널을 여러 개로 쪼개는 대신 이걸 씁니다** |
| `SectionLabel` | Bold 12sp, 자간 +0.6 | 패널 위 섹션 이름 |
| `ConditionSettingCard` | 토글 + 구분선 + 스테퍼 | 설정의 조건 카드 3개 |

**카드 지옥을 피하는 것이 이 앱의 규칙입니다** — 중첩된 카드를 만들지 말고,
`SlPanel` 하나 안에서 `SlDivider`와 여백으로 나눕니다 **[코드 + 화면]**.

## 4-3. 목록 줄 **[코드]**

| 컴포넌트 | 구조 | 쓰는 기준 |
| --- | --- | --- |
| `ConditionRow` | 앞쪽 슬롯 + 제목/값 + 뒤쪽 슬롯 | 홈의 조건 줄 |
| `AppListItem` | 앱 뱃지 + 이름(+부제) + 뒤쪽 슬롯 | 앱 목록. **홈은 부제 없이, 설정은 체크박스와 함께** |
| `SlDetailRow` | 제목 + 설명 + 뒤쪽 슬롯 | **제목·설명 한 줄의 배치 원시 요소.** 면과 뜻은 호출하는 쪽이 정합니다 |
| `SlEmptyState` | `SlDetailRow` + 셰브론(옵션) | 패널에 보여 줄 내용이 없을 때 |

`SlDetailRow`를 쓰는 곳 — 뜻은 다르지만 **배치가 같습니다** **[코드]**

| 어디 | 면 | 뒤쪽 |
| --- | --- | --- |
| 빈 상태 | 패널 안 (면 없음) | 셰브론(누를 수 있을 때만) |
| 권한 경고 (홈) | `Surface` + `Error` 테두리 | 셰브론 |
| 계정 줄 (설정) | `SurfaceAlt` | `TextLink` |
| 전부 만족 토글 (설정) | `SurfaceAlt` | `SlSwitch` |

**뜻이 다르므로 색과 뒤쪽 컨트롤은 다릅니다.** 배치만 공유해서 제목·설명의
글자 크기와 간격이 저절로 맞습니다.

**같은 역할의 줄은 같은 앞쪽 요소를 씁니다.** 홈의 조건 세 줄은 걸음·수면·집중이
모두 "목표 대비 진행"이므로 **같은 44dp 링**을 쓰고, 달성하면 숫자 대신 체크가 들어갑니다.
이전에는 링·원+체크·사각 타일로 셋이 달라 규칙이 없어 보였습니다 **[코드 + 화면]**.

## 4-4. 상태 표시 **[코드]**

| 컴포넌트 | 모양 | 쓰는 기준 |
| --- | --- | --- |
| `ProgressRing` | 원형 게이지, 박스 크기와 반지름을 따로 받음 | 홈 **44dp** / 잠금 **176dp** |
| `StatusChip` | 캡슐, 달성/미달성 2상태 | 잠금 화면의 남은 조건 |
| `SlChevron` | 18dp 셰브론, `TextTertiary` | **눌리는 줄에만** 붙입니다 |
| `WeeklyBarChart` | 막대 하나에 값 하나 | 통계. 영점에서 시작하고 목표는 점선으로만 |
| `SlSwitch` · `CheckboxMark` | 52×32 / 24dp | 켜기·고르기 |
| `SlSegmented` | 알약 안의 2~3칸, 고른 칸만 `Surface` | **보는 범위만** 바꿉니다. 화면 이동은 하단 탭바 |
| `AppBadge` | 40dp/36dp 둥근 사각 + 머리글자 | 앱 식별 |
| `StepLockMascot` | Canvas로 그린 캐릭터 | 브랜드. 색은 토큰으로 받습니다 |

### 캐릭터를 쓰는 자리 **[코드]**

자세가 상태를 말합니다 — 글자를 읽지 않아도 지금 무슨 상태인지 보이게.

| 자리 | 크기 | 자세 |
| --- | --- | --- |
| 온보딩 | 96×119dp | `Walking` |
| 홈 (`StepTrack` 위) | 44×55dp | 미달 `Walking` / 달성 `Resting` |
| 잠금 | 80×99dp | 미달 `Walking` / 달성 `Resting` (다크 팔레트) |
| 집중 타이머 | 56×69dp | 진행 중 `Focusing` / 멈춤·대기 `Resting` |
| 빈 상태 (`SlEmptyState`) | 36×45dp | `Resting` |
| 스플래시 | 288×288 벡터 | `Resting` (정지 — `ic_splash_mascot.xml`) |
| 앱·알림 아이콘 | 적응형 / 24dp | 자물쇠 기호 (캐릭터 아님) |

**자세 세 가지**

| 자세 | 뜻 | 움직임 |
| --- | --- | --- |
| `Walking` | 목표까지 남음 | 다리 스윙 + 상하 반동, 900ms |
| `Resting` | 달성·대기 | **완전 정지.** 애니메이션 값을 읽지 않습니다 |
| `Focusing` | 집중 세션 진행 중 | 앉은 자세, 숨결만 2600ms 왕복 |

**넣지 않는 곳** — 로그인(워드마크와 겹침), 설정(작업 중), 통계 차트 안(데이터를
가림), 목표 달성 축하 연출(`Resting` 으로 이미 표현됩니다).

## 4-5. 입력 **[코드]**

| 컴포넌트 | 모양 | 상태 |
| --- | --- | --- |
| `UnderlineTextField` | 밑줄형 (테두리 없음) | 기본 `Border` → **포커스 시 밑줄이 `Brand`**. 비밀번호는 표시 토글 내장 |

입력 칸은 **로그인 화면에만** 있습니다. 새 입력 칸이 필요하면 이 컴포넌트를 쓰세요.

## 4-6. 대화상자 **[코드]**

| 컴포넌트 | 쓰는 기준 |
| --- | --- |
| `SlConfirmDialog` | **되돌릴 수 없는 동작**만. Material3 기본 대화상자는 팔레트가 따로 돌아 앱 토큰으로 직접 그렸습니다 |

## 4-7. 추출하지 않은 것

다음은 **뜻이 한 화면에만 있어서** private으로 둡니다. 배치는 이미 공통 요소를 쓰므로
중복이 아닙니다. 두 번째 화면에서 같은 뜻이 필요해지면 그때 `ui/components/`로 올리세요.

| 무엇 | 어디 | 안에서 쓰는 공통 요소 |
| --- | --- | --- |
| `PermissionWarning` | `HomeScreen.kt` | `SlPanel` + `SlDetailRow` + `SlChevron` |
| `AccountRow` · `StrictModeRow` | `SettingsScreen.kt` | `SlPanel` + `SlDetailRow` |
| `AchievementRow` | `StatsScreen.kt` | `ConditionRow` + `IconTile` |
| `TodayStatusCard` · `ConditionRing` | `HomeScreen.kt` | `StepLockMascot` / `ProgressRing` |
| `UnlockRule` | `OnboardingScreen.kt` | `IconTile` — 아이콘이 **앞**에 오는 다른 구조 |

## 4-8. iOS 대응 **[제안 · 시작값]**

| Android | iOS 시작값 |
| --- | --- |
| `PrimaryButton` | `UIButton`(filled) 또는 SwiftUI `Button` + `.buttonStyle(.borderedProminent)`, 높이 50pt |
| `SlPanel` | `UIView` + `cornerRadius 12` + 1pt border, 또는 SwiftUI `.background(...).clipShape(...)` |
| `SlDivider` | `Divider()` (기본 1pt) |
| `UnderlineTextField` | `TextField` + 하단 1pt 선. iOS 기본 `.roundedBorder`는 쓰지 않습니다 |
| `SlConfirmDialog` | `.confirmationDialog(role: .destructive)` — iOS는 시스템 대화상자가 관례라 직접 그리지 않습니다 |
| `SlSwitch` | `Toggle` (시스템 기본). 52×32 커스텀을 옮기지 않습니다 |

**iOS는 시스템 컨트롤을 그대로 쓰는 쪽이 자연스럽습니다.** Android에서 직접 그린 스위치·
대화상자를 억지로 이식하지 마세요. 색과 글꼴만 맞추면 됩니다.

---

# 5. 화면 상태

## 5-1. 현재 구현 상태 **[코드]**

| 상태 | 구현 여부 | 어떻게 |
| --- | --- | --- |
| 기본 | ○ | — |
| 눌림 | △ | **Material3 기본 리플**만. 전용 색 없음 |
| 선택 | ○ | 체크박스(`CheckboxMark`), 스위치(`SlSwitch`), 하단 탭(`NavLabelActive` + `Brand`) |
| 포커스 | ○ | `UnderlineTextField`만 — 밑줄이 `Border` → `Brand` |
| 사용할 수 없음 | ○ | `PrimaryButton` — 면 `TrackOff`, 글자 `TextSecondary` |
| 로딩 | △ | **버튼 글자만 교체** ("로그인" → "로그인 중…") + 버튼 비활성. 스피너 없음 |
| 내용 없음 | ○ | `SlEmptyState` — 홈(조건/앱 없음), 통계(기록 없음) |
| 오류 | ○ | 입력 오류는 `Error` 글자, 화면 수준은 빨간 테두리 경고 줄 |

## 5-2. 기준

### 기본 → 눌림 **[제안 · 시작값]**

지금은 리플에 맡깁니다. **기기에서 확인한 뒤** 어색하면:
면 색을 한 단계 바꾸고(1-5 표), 크기 변화나 그림자는 쓰지 않습니다.
이 앱은 그림자를 거의 쓰지 않습니다 **[코드]**.

### 포커스 **[코드]**

입력 칸은 **밑줄 색만** 바꿉니다. 테두리를 두르거나 배경을 채우지 않습니다.

### 사용할 수 없음 **[코드]**

- 버튼: 면 `TrackOff` + 글자 `TextSecondary`
- **눌러도 안 되는 버튼을 남기지 않습니다.** 잠금 화면의 임시 허용은 한도를 다 쓰면
  버튼을 **없애고** 이유를 글로 적습니다 — 비활성 버튼보다 이유를 말하는 쪽이 낫습니다.

### 로딩 **[코드 + 제안]**

현재: 버튼 글자를 진행형으로 바꾸고 비활성화합니다. **[코드]**
**시작값 제안**: 1초 이상 걸릴 수 있는 동작은 버튼 안에 16dp 원형 인디케이터를 넣고,
화면 전체를 덮는 로딩은 쓰지 않습니다. 화면을 가리면 취소할 방법이 사라집니다.

### 내용 없음 **[코드]**

`SlPanel` 안에 `SlEmptyState`를 넣습니다. **빈 면을 그대로 두지 않습니다** — 고장처럼 보입니다.

- 사용자가 고칠 수 있으면 `onClick`을 주어 셰브론을 붙입니다 (예: "설정에서 조건을 켜면…")
- 그냥 기다려야 하면 `onClick`을 비웁니다 (예: 통계 "하루를 보내면 여기에 쌓입니다")

### 오류 **[코드]**

두 층으로 나뉩니다.

| 층 | 모양 | 예 |
| --- | --- | --- |
| **입력 오류** | 입력 칸 아래 `Error` 색 `Caption` | "이메일 주소를 다시 확인해 주세요" |
| **기능 멈춤** | `Surface` 면 + 1dp `Error` 테두리 + `Error` 제목 + 셰브론 | "잠금이 멈춰 있어요 / 권한이 꺼졌어요" |

**거짓 상태를 보여주지 않습니다.** 권한이 꺼져 감시가 멈추면 "감지 중" 초록 점을
**숨깁니다** — 켜 둔 채로 두면 화면이 거짓말을 합니다 **[코드]**.

### 문구 규칙 **[코드]**

- 버튼은 **무엇이 일어나는지** 적습니다. "확인"/"취소"를 쓰지 않습니다.
  → "손쉬운 사용 권한 허용하기", "계정과 기록 삭제하기", "그대로 두기", "알겠어요, 닫기"
- 한국어 조사를 받침에 따라 붙입니다 — "쇼츠**는**", "틱톡**은**" (`withTopicParticle`)
- 재촉하지 않습니다. 지금 당장 할 수 없는 조건(수면)은 숫자로 남은 양을 보여주지 않습니다.

## 5-3. iOS 대응 **[제안 · 시작값]**

| 상태 | iOS 시작값 |
| --- | --- |
| 눌림 | `.opacity(0.7)` 또는 시스템 `highlighted` — iOS는 리플이 없습니다 |
| 포커스 | 밑줄 `Brand`로 동일 |
| 사용할 수 없음 | `.disabled(true)` + `.opacity(0.4)` |
| 로딩 | `ProgressView()` 16pt |
| 내용 없음 | `ContentUnavailableView` (iOS 17+) 또는 같은 구조를 직접 |
| 오류 | 입력 아래 빨간 캡션 + 화면 상단 경고 카드 |

---

# 6. 실제 화면 적용 예시

## 6-1. 홈 — 상태를 먼저, 조건은 한 가지 모양으로 **[코드 + 화면]**

`docs/screens/03-home.png`

```
[ScreenPadding 20dp]
좋은 아침이에요, 지우님        ← Greeting 24sp Black / TextPrimary
9월 21일 월요일               ← RowValue 13sp Normal / TextSecondary   (간격 6dp)
                              ↕ 24dp
┌ RadiusPanel 20dp · AmberSurface ─────────────┐
│ [캐릭터 60×74]  아직 잠겨 있어요               │ ← StatusTitle 18sp Black / AmberText
│                2,760보만 더 걸으면 열려요      │ ← BodySm 13sp / AmberSubText  (6dp)
│                ( 5일 연속 )                   │ ← Chip 12sp Bold / Surface 캡슐  (10dp)
└───────────────────────────────────────────────┘
                              ↕ 28dp
잠금 해제 조건                 ← SectionLabel 12sp Bold 자간+0.6
                              ↕ 12dp
┌ SlPanel · RadiusCard 18dp ───────────────────┐
│ (링 44dp 66%) 걸음 수                         │ ← RowTitle 15sp Bold
│               5,240 / 8,000보                 │ ← RowValue 13sp / TextSecondary
│ ──── SlDivider ──────────────────────────────│
│ (링 44dp ✓)  수면 시간                        │ ← 달성이면 숫자 대신 체크
│ ──── SlDivider ──────────────────────────────│
│ (링 44dp 67%) 집중 타이머          [셰브론]   │ ← 눌리는 줄에만 셰브론
└───────────────────────────────────────────────┘
```

**적용된 기준**

- 상태 카드는 **주의(앰버)** 색을 씁니다. 잠긴 것은 정상 상태이므로 오류 빨강이 아닙니다.
- 열리면 면이 `BrandTint`, 글자가 `BrandDeep`으로 바뀌고 캐릭터가 눈을 감습니다 —
  **색과 형태가 함께 바뀌어 글을 읽지 않아도 구분됩니다.**
- 조건 세 줄은 **같은 44dp 링**입니다. 상태만 다르고 모양은 같습니다.
- "감지 중" 표시는 섹션 헤더에 **한 번만** — 앱별 상태가 아니라 감시 서비스 하나의 상태입니다.

## 6-2. 잠금 — 조건 하나에 집중 **[코드 + 화면]**

`docs/screens/05-lock.png`

```
[ScreenPaddingWide 24dp · Dark.Background]
        [캐릭터 80×99 · Dark.GreenIcon]        ← 다크 전용 그린 (반전 아님)
                    ↕ 24dp
        쇼츠는 잠겨있어요                       ← LockTitle 25sp Black / Dark.TextPrimary
                    ↕ 8dp
        2,760보만 더 걸으면 열려요              ← Remaining 14sp Bold / Dark.AmberText
                    ↕ 32dp
        ( 링 176dp · 66% · Dark.AmberRing )     ← 채도 낮춘 앰버
                    ↕ 28dp
        [수면 완료]  [2/3 세션]                 ← StatusChip, 링으로 보여 준 조건은 제외
                    ↕ 12dp
        조건을 모두 채워야 열려요                ← LabelSm 12sp / Dark.TextMuted
                    ↕ (남는 공간)
        [ 알겠어요, 닫기 ]                      ← PrimaryButton 56dp · Dark.SurfaceAlt
        5분만 임시로 허용하기 (오늘 3번 남음)     ← TextLink · 한도 소진 시 사라짐
```

**적용된 기준**

- `ScreenPaddingWide` — 하단 탭 없는 단독 화면입니다.
- 주 버튼 하나 + 텍스트 링크 하나. **닫기 수단을 여러 개 두지 않습니다**
  (이전에는 우상단 X까지 있어 셋이었습니다).
- 링 채도를 낮춘 이유는 **가장 큰 면적**이기 때문입니다.

## 6-3. 설정 — 같은 구조의 카드 세 개 **[코드 + 화면]**

`docs/screens/04-settings.png`

```
┌ ConditionSettingCard · RadiusCard 18dp ──────┐
│ [타일 40dp] 걸음 수            [스위치 52×32] │ ← RowTitle + SlSwitch
│ ──── SlDivider ──────────────────────────────│
│ 하루 목표      [−44dp] 8,000보 [+44dp]        │ ← StepperLabel + StepperValue
└───────────────────────────────────────────────┘
                              ↕ 16dp   (카드 사이)
        … 수면 시간 카드 (같은 구조)
        … 집중 타이머 카드 (같은 구조)
```

조건 셋은 **완전히 같은 구조**입니다. 단위만 다릅니다(500보 / 30분 / 1회).
역할이 같으므로 모양을 맞췄습니다.

## 6-4. 새 화면을 만들 때 **[제안]**

1. 좌우 여백: 하단 탭이 있으면 `ScreenPadding`, 없으면 `ScreenPaddingWide`
2. 내용은 `SlPanel` 안에 넣고 줄은 `SlDivider`로 나눕니다 — **새 카드를 겹치지 않습니다**
3. 섹션은 `SectionLabel` + 28dp 위 여백
4. 주 동작은 `PrimaryButton` **하나**, 나머지는 `TextLink`
5. 누를 수 있으면 최소 44dp, 눌리는 줄에는 `SlChevron`
6. 빈 상태·오류 상태를 먼저 정합니다 (5절)
7. 새 색·크기가 필요하면 토큰에 추가하고 **이 문서에 적습니다**

---

# 7. 광고 배치 기준 **[코드]**

수익이 아니라 **사용자가 무엇을 하는 중인지**로 자리를 정합니다.

## 7-1. 넣지 않는 화면

| 화면 | 이유 |
| --- | --- |
| 잠금 (`LockOverlayScreen`) | 배너·전면 광고 금지. 참는 중인 사람에게 "막았고, 광고도 보세요"가 됩니다 |
| 집중 타이머 (`PomodoroScreen`) | 25분 집중하겠다고 켠 화면입니다. 광고가 앱의 목적을 그대로 깨뜨립니다 |
| 온보딩 · 로그인 | 첫인상이자 이탈이 가장 많은 구간입니다 |
| 홈 | 하루에 여러 번 스치듯 보는 화면이고, `StepTrack`이 이 앱의 얼굴입니다 |
| 설정 | 무언가를 바꾸는 중입니다 |

전면 광고(Interstitial)는 **어디에도 쓰지 않습니다.** 한 번 쓰는 시간이 10초 남짓이라
자연스러운 끊김 지점이 없고, 유일한 후보(집중 세션 완료)는 위 이유로 막혀 있습니다.

## 7-2. 넣는 자리

| 자리 | 형태 | 조건 |
| --- | --- | --- |
| 통계 화면 하단 (`StatsScreen`) | 앵커드 어댑티브 배너 (`SlBannerAd`) | 기록이 하나라도 있을 때만 |
| 잠금 화면 하단 링크 (`LockOverlayScreen`) | 리워드 광고 | 기본 임시 허용 3회를 **다 쓴 뒤에만** |

배너는 위에 1dp `Border` 선을 두어 앱 내용과 경계를 분명히 합니다 — 광고가 앱
화면의 일부로 읽히면 안 됩니다.

리워드 광고 링크는 기존 `TextLink`를 그대로 씁니다. 광고라서 더 눈에 띄게 만들지
않습니다. 문구는 얻는 것을 먼저 적습니다 — "광고 보기"(X) → "광고 보고 5분 더
받기"(O).

## 7-3. 상한

`TemporaryAllow.DAILY_LIMIT` 3 + `AD_BONUS_LIMIT` 2 = **하루 최대 5회**.

상한이 없으면 광고를 계속 보면서 잠금을 무력화할 수 있습니다. 광고 수익보다
앱의 목적이 먼저이므로 여기서 끊습니다.

## 7-4. iOS 대응 **[제안 · 시작값]**

| 항목 | 값 |
| --- | --- |
| 배너 자리 | 탭 바 **위**, `safeAreaInset(edge: .bottom)` |
| 배너 크기 | `currentOrientationAnchoredAdaptiveBanner(width:)` |
| 경계선 | 1pt `Border` (Android 1dp와 동일 역할) |
| 추적 동의 | ATT 권한 요청을 UMP 동의 **뒤에** 한 번 |

iOS 코드는 아직 없습니다. 값은 Android 구현을 그대로 옮긴 제안입니다.

---

# 부록: 이 문서에서 새로 정한 것

| 항목 | 이전 | 정한 값 | 종류 |
| --- | --- | --- | --- |
| 좌우 여백 이름 | 20/24/28dp 혼용 | `ScreenPadding` 20 · `ScreenPaddingWide` 24 | 기존 값 정리 |
| 로그인 여백 | 28dp | 24dp | 통일 |
| 로그인 버튼 높이 | 52dp | 56dp | 통일 |
| 셰브론 | 홈에 private | `SlChevron` 공통 | 추출 |
| 빈 상태 | 홈에 private | `SlEmptyState` 공통 | 추출 |
| 통계 빈 상태 | 없음 | `SlEmptyState` 적용 | 신규 |
| 눌림 색 | 없음 (리플) | 1-5절 표 | **[제안 · 시작값]** |
| 버튼 안 로딩 표시 | 없음 | 16dp 인디케이터 | **[제안 · 시작값]** |
| 고정폭 숫자 | 없음 | 타이머에 적용 | **[제안 · 시작값]** |
| iOS 전체 | 없음 | 각 절의 iOS 표 | **[제안 · 시작값]** |
| 제목+설명 줄 | 네 곳에서 따로 그림 | `SlDetailRow` 공통 | 추출 |
| 줄 설명 글자 | 12sp · 13sp 혼용 | 13sp `RowValue` | 통일 |
| 보조 면 카드 | 설정에서 직접 그림 | `SlPanel(containerColor)` | 통일 |
| 로그인 텍스트 버튼 | 사설 `InlineTextButton` | 공통 `TextLink` | 통일 |
| 텍스트 링크 리플 | 모서리 없는 사각형 | 8dp 모서리 + 좌우 4dp | 개선 |
| `RadiusCheckbox` | 체크박스 전용 이름 | `RadiusSmall` (리플에도 씀) | 이름 정리 |
| 광고 배치 | 없음 | 7절 (통계 배너 + 잠금 리워드만) | 신규 |
| 캐릭터 자세 | 걷기·정지 둘 | `Focusing` 추가 (앉아서 숨결) | 신규 |
| 기간 전환 | 없음 (7일 고정) | `SlSegmented` 7일/30일 | 신규 |
| 완화 대기 안내 | 없음 | 앰버 패널 (오류도 완료도 아닌 "기다리는 중") | 신규 |
| 대기 기간 고르기 | 없음 | `SlSegmented` 4칸 (바로/내일/3일/7일) | 신규 |
| 조건별 기록 | 7개 점 | 평균 + 달성률 + 달성 일수 | 개선 |
| 걸음 수 요약 | 캡션 한 줄 | 일평균·최고·목표 달성 3칸 | 개선 |
| 꺼 둔 조건 표시 | 구분 없음 | 아이콘 흐리게 + `· 꺼짐` | 신규 |
| 캐릭터 쓰는 자리 | 문서화 안 됨 | 4-4절 표 | 정리 + 신규 3곳 |
| 빈 상태 | 글자만 | 캐릭터 36×45dp 추가 | 개선 |
| 스플래시 화면 | 없음 (흰 화면) | 캐릭터 벡터 + 크림 배경 | 신규 |

**[제안 · 시작값]은 아직 코드에 없습니다.** 실제 기기에서 확인한 뒤 필요한 것만 넣으세요.
